name: Build and Release

# vから始まるGitタグがpushされたときに実行します。
on:
  push:
    tags:
      - "v*"

# GitHub Releaseの作成に必要です。
permissions:
  contents: write

jobs:
  build-and-release:
    name: Build and release Linux x64 applications
    runs-on: ubuntu-latest

    steps:
      # ============================================================
      # 共通準備
      # ============================================================

      # Repositoryのソースコードを取得します。
      - name: Checkout repository
        uses: actions/checkout@v7

      # ビルド、テスト、publishに使用する.NET 10 SDKを準備します。
      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"

      # 使用する.NET SDKとRuntimeの情報を表示します。
      - name: Display .NET information
        run: dotnet --info

      # Gitタグの先頭にあるvを除去し、
      # debパッケージと.NETアプリのバージョンに使用します。
      #
      # 例:
      #   v0.1.0
      #     ↓
      #   0.1.0
      - name: Set version from Git tag
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"

          if [[ ! "${VERSION}" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid version tag: ${GITHUB_REF_NAME}"
            exit 1
          fi

          echo "VERSION=${VERSION}" >> "${GITHUB_ENV}"
          echo "Version: ${VERSION}"

      # debパッケージの作成に使用するツールをインストールします。
      - name: Install Debian package tools
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev

      # SBOM生成用のMicrosoft sbom-toolをインストールします。
      - name: Install SBOM tool
        run: |
          dotnet tool install --global Microsoft.Sbom.DotNetTool

      # SBOMからOSSライセンスNoticeを生成するためにONOTをインストールします。
      - name: Install ONOT
        shell: bash
        run: |
          python3 -m venv "${RUNNER_TEMP}/onot-venv"
          source "${RUNNER_TEMP}/onot-venv/bin/activate"

          pip install --upgrade pip
          pip install "onot[spdx,cyclonedx,excel,api]==1.1.2"

      # ============================================================
      # ソリューション全体のテスト
      # ============================================================

      # Repositoryルートにあるソリューションを復元します。
      - name: Restore solution
        run: dotnet restore

      # ソリューション全体をRelease構成でビルドします。
      - name: Build solution
        run: >
          dotnet build
          --configuration Release
          --no-restore
          -p:Version=${{ env.VERSION }}
          -p:SourceRevisionId=${{ github.sha }}
          -p:ContinuousIntegrationBuild=true

      # Unit Testを実行します。
      - name: Run tests
        run: >
          dotnet test
          --configuration Release
          --no-build
          --no-restore

      # ============================================================
      # LinuxEdgeInspection.Runtime
      # ============================================================

      # linux-x64向けpublishに必要な依存関係を復元します。
      - name: Restore camera runtime dependencies
        run: >
          dotnet restore
          src/LinuxEdgeInspection.Runtime/LinuxEdgeInspection.Runtime.csproj
          --runtime linux-x64

      # LinuxEdgeInspection.Runtimeをlinux-x64向けにpublishします。
      #
      # framework-dependent構成のため、実機には
      # .NET 10 Runtimeが必要です。
      - name: Publish camera runtime
        run: >
          dotnet publish
          src/LinuxEdgeInspection.Runtime/LinuxEdgeInspection.Runtime.csproj
          --configuration Release
          --runtime linux-x64
          --self-contained false
          --no-restore
          --output artifacts/publish/linux-edge-inspection-runtime
          -p:Version=${{ env.VERSION }}
          -p:SourceRevisionId=${{ github.sha }}
          -p:ContinuousIntegrationBuild=true

      # 配布不要な開発用ファイルを削除します。
      - name: Clean camera runtime publish output
        shell: bash
        run: |
          rm -f \
            artifacts/publish/linux-edge-inspection-runtime/appsettings.Development.json

          find artifacts/publish/linux-edge-inspection-runtime \
            -maxdepth 1 \
            -type f \
            -name "*.pdb" \
            -delete

      # RuntimeのSBOM出力先を作成します。
      - name: Create camera runtime SBOM directory
        run: |
          mkdir -p artifacts/sbom/runtime

      # Runtimeのpublish成果物を基準にSBOMを生成します。
      - name: Generate camera runtime SBOM
        shell: bash
        run: |
          sbom-tool generate \
            -b "${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-runtime" \
            -m "${GITHUB_WORKSPACE}/artifacts/sbom/runtime" \
            -pn "LinuxEdgeInspection.Runtime" \
            -pv "${VERSION}" \
            -ps "mono-tec"

      # RuntimeのSBOMからOSSライセンスNoticeを生成します。
      - name: Generate camera runtime OSS notice
        shell: bash
        run: |
          source "${RUNNER_TEMP}/onot-venv/bin/activate"

          rm -rf artifacts/license/linux-edge-inspection-runtime
          mkdir -p artifacts/license/linux-edge-inspection-runtime

          onot generate \
            -i artifacts/sbom/runtime/_manifest/spdx_2.2/manifest.spdx.json \
            -f markdown \
            --output-dir artifacts/license/linux-edge-inspection-runtime

      # Runtimeのライセンス関連ファイルを
      # Debianパッケージへ格納しやすい形へ整理します。
      - name: Prepare camera runtime compliance files
        shell: bash
        run: |
          COMPLIANCE_DIR="artifacts/compliance/linux-edge-inspection-runtime"

          rm -rf "${COMPLIANCE_DIR}"
          mkdir -p "${COMPLIANCE_DIR}"

          cp LICENSE "${COMPLIANCE_DIR}/LICENSE"

          NOTICE_FILE="$(find \
            artifacts/license/linux-edge-inspection-runtime \
            -maxdepth 1 \
            -type f \
            -name "*.md" \
            | head -n 1)"

          if [[ -z "${NOTICE_FILE}" ]]; then
            echo "Runtime OSS Notice file was not generated."
            exit 1
          fi

          cp "${NOTICE_FILE}" "${COMPLIANCE_DIR}/THIRD-PARTY-NOTICES.md"

          cp \
            artifacts/sbom/runtime/_manifest/spdx_2.2/manifest.spdx.json \
            "${COMPLIANCE_DIR}/sbom.spdx.json"

          echo "Runtime compliance files:"
          find "${COMPLIANCE_DIR}" -maxdepth 1 -type f -print

      # publish成果物からRuntime用debパッケージを作成します。
      - name: Create camera runtime Debian package
        shell: bash
        run: |
          chmod +x packaging/runtime-deb/build-deb.sh

          PACKAGE_VERSION="${VERSION}" \
          PACKAGE_ARCHITECTURE="amd64" \
          RUNTIME_IDENTIFIER="linux-x64" \
          PUBLISH_DIR="${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-runtime" \
          COMPLIANCE_DIR="${GITHUB_WORKSPACE}/artifacts/compliance/linux-edge-inspection-runtime" \
          ./packaging/runtime-deb/build-deb.sh

      # ============================================================
      # LinuxEdgeInspection.CaptureRequestListener
      # ============================================================

      # linux-x64向けpublishに必要な依存関係を復元します。
      - name: Restore Capture Request Listener dependencies
        run: >
          dotnet restore
          src/LinuxEdgeInspection.CaptureRequestListener/LinuxEdgeInspection.CaptureRequestListener.csproj
          --runtime linux-x64

      # LinuxEdgeInspection.CaptureRequestListenerを
      # linux-x64向けにpublishします。
      - name: Publish Capture Request Listener
        run: >
          dotnet publish
          src/LinuxEdgeInspection.CaptureRequestListener/LinuxEdgeInspection.CaptureRequestListener.csproj
          --configuration Release
          --runtime linux-x64
          --self-contained false
          --no-restore
          --output artifacts/publish/linux-edge-inspection-capture-request-listener
          -p:Version=${{ env.VERSION }}
          -p:SourceRevisionId=${{ github.sha }}
          -p:ContinuousIntegrationBuild=true

      # 配布不要な開発用ファイルを削除します。
      - name: Clean Capture Request Listener publish output
        shell: bash
        run: |
          rm -f \
            artifacts/publish/linux-edge-inspection-capture-request-listener/appsettings.Development.json

          find artifacts/publish/linux-edge-inspection-capture-request-listener \
            -maxdepth 1 \
            -type f \
            -name "*.pdb" \
            -delete

      # Capture Request ListenerのSBOM出力先を作成します。
      - name: Create Capture Request Listener SBOM directory
        run: |
          mkdir -p artifacts/sbom/capture-request-listener

      # Capture Request Listenerのpublish成果物を基準にSBOMを生成します。
      - name: Generate Capture Request Listener SBOM
        shell: bash
        run: |
          sbom-tool generate \
            -b "${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-capture-request-listener" \
            -m "${GITHUB_WORKSPACE}/artifacts/sbom/capture-request-listener" \
            -pn "LinuxEdgeInspection.CaptureRequestListener" \
            -pv "${VERSION}" \
            -ps "mono-tec"

      # Capture Request ListenerのSBOMからOSSライセンスNoticeを生成します。
      - name: Generate Capture Request Listener OSS notice
        shell: bash
        run: |
          source "${RUNNER_TEMP}/onot-venv/bin/activate"

          rm -rf artifacts/license/linux-edge-inspection-capture-request-listener
          mkdir -p artifacts/license/linux-edge-inspection-capture-request-listener

          onot generate \
            -i artifacts/sbom/capture-request-listener/_manifest/spdx_2.2/manifest.spdx.json \
            -f markdown \
            --output-dir artifacts/license/linux-edge-inspection-capture-request-listener

      # Capture Request Listenerのライセンス関連ファイルを
      # Debianパッケージへ格納しやすい形へ整理します。
      - name: Prepare Capture Request Listener compliance files
        shell: bash
        run: |
          COMPLIANCE_DIR="artifacts/compliance/linux-edge-inspection-capture-request-listener"

          rm -rf "${COMPLIANCE_DIR}"
          mkdir -p "${COMPLIANCE_DIR}"

          cp LICENSE "${COMPLIANCE_DIR}/LICENSE"

          NOTICE_FILE="$(find \
            artifacts/license/linux-edge-inspection-capture-request-listener \
            -maxdepth 1 \
            -type f \
            -name "*.md" \
            | head -n 1)"

          if [[ -z "${NOTICE_FILE}" ]]; then
            echo "Capture Request Listener OSS Notice file was not generated."
            exit 1
          fi

          cp "${NOTICE_FILE}" "${COMPLIANCE_DIR}/THIRD-PARTY-NOTICES.md"

          cp \
            artifacts/sbom/capture-request-listener/_manifest/spdx_2.2/manifest.spdx.json \
            "${COMPLIANCE_DIR}/sbom.spdx.json"

          echo "Capture Request Listener compliance files:"
          find "${COMPLIANCE_DIR}" -maxdepth 1 -type f -print

      # publish成果物からCapture Request Listener用debパッケージを作成します。
      - name: Create Capture Request Listener Debian package
        shell: bash
        run: |
          chmod +x packaging/capture-request-listener-deb/build-deb.sh

          PACKAGE_VERSION="${VERSION}" \
          PACKAGE_ARCHITECTURE="amd64" \
          RUNTIME_IDENTIFIER="linux-x64" \
          PUBLISH_DIR="${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-capture-request-listener" \
          COMPLIANCE_DIR="${GITHUB_WORKSPACE}/artifacts/compliance/linux-edge-inspection-capture-request-listener" \
          ./packaging/capture-request-listener-deb/build-deb.sh


      # ============================================================
      # LinuxEdgeInspection.InspectionWorker
      # ============================================================

      # linux-x64向けpublishに必要な依存関係を復元します。
      - name: Restore Inspection Worker dependencies
        run: >
          dotnet restore
          src/LinuxEdgeInspection.InspectionWorker/LinuxEdgeInspection.InspectionWorker.csproj
          --runtime linux-x64

      # LinuxEdgeInspection.InspectionWorkerを
      # linux-x64向けにpublishします。
      #
      # framework-dependent構成のため、実機には
      # .NET 10 Runtimeが必要です。
      - name: Publish Inspection Worker
        run: >
          dotnet publish
          src/LinuxEdgeInspection.InspectionWorker/LinuxEdgeInspection.InspectionWorker.csproj
          --configuration Release
          --runtime linux-x64
          --self-contained false
          --no-restore
          --output artifacts/publish/linux-edge-inspection-inspection-worker
          -p:Version=${{ env.VERSION }}
          -p:SourceRevisionId=${{ github.sha }}
          -p:ContinuousIntegrationBuild=true

      # 配布不要な開発用ファイルを削除します。
      - name: Clean Inspection Worker publish output
        shell: bash
        run: |
          rm -f \
            artifacts/publish/linux-edge-inspection-inspection-worker/appsettings.Development.json

          find artifacts/publish/linux-edge-inspection-inspection-worker \
            -maxdepth 1 \
            -type f \
            -name "*.pdb" \
            -delete

      # Inspection WorkerのSBOM出力先を作成します。
      - name: Create Inspection Worker SBOM directory
        run: |
          mkdir -p artifacts/sbom/inspection-worker

      # Inspection Workerのpublish成果物を基準にSBOMを生成します。
      - name: Generate Inspection Worker SBOM
        shell: bash
        run: |
          sbom-tool generate \
            -b "${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-inspection-worker" \
            -m "${GITHUB_WORKSPACE}/artifacts/sbom/inspection-worker" \
            -pn "LinuxEdgeInspection.InspectionWorker" \
            -pv "${VERSION}" \
            -ps "mono-tec"

      # Inspection WorkerのSBOMからOSSライセンスNoticeを生成します。
      - name: Generate Inspection Worker OSS notice
        shell: bash
        run: |
          source "${RUNNER_TEMP}/onot-venv/bin/activate"

          rm -rf artifacts/license/linux-edge-inspection-inspection-worker
          mkdir -p artifacts/license/linux-edge-inspection-inspection-worker

          onot generate \
            -i artifacts/sbom/inspection-worker/_manifest/spdx_2.2/manifest.spdx.json \
            -f markdown \
            --output-dir artifacts/license/linux-edge-inspection-inspection-worker

      # Inspection Workerのライセンス関連ファイルを
      # Debianパッケージへ格納しやすい形へ整理します。
      - name: Prepare Inspection Worker compliance files
        shell: bash
        run: |
          COMPLIANCE_DIR="artifacts/compliance/linux-edge-inspection-inspection-worker"

          rm -rf "${COMPLIANCE_DIR}"
          mkdir -p "${COMPLIANCE_DIR}"

          cp LICENSE "${COMPLIANCE_DIR}/LICENSE"

          NOTICE_FILE="$(find \
            artifacts/license/linux-edge-inspection-inspection-worker \
            -maxdepth 1 \
            -type f \
            -name "*.md" \
            | head -n 1)"

          if [[ -z "${NOTICE_FILE}" ]]; then
            echo "Inspection Worker OSS Notice file was not generated."
            exit 1
          fi

          cp \
            "${NOTICE_FILE}" \
            "${COMPLIANCE_DIR}/THIRD-PARTY-NOTICES.md"

          cp \
            artifacts/sbom/inspection-worker/_manifest/spdx_2.2/manifest.spdx.json \
            "${COMPLIANCE_DIR}/sbom.spdx.json"

          echo "Inspection Worker compliance files:"
          find "${COMPLIANCE_DIR}" -maxdepth 1 -type f -print

      # publish成果物からInspection Worker用debパッケージを作成します。
      - name: Create Inspection Worker Debian package
        shell: bash
        run: |
          chmod +x packaging/inspection-worker-deb/build-deb.sh

          PACKAGE_VERSION="${VERSION}" \
          PACKAGE_ARCHITECTURE="amd64" \
          RUNTIME_IDENTIFIER="linux-x64" \
          PUBLISH_DIR="${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-inspection-worker" \
          COMPLIANCE_DIR="${GITHUB_WORKSPACE}/artifacts/compliance/linux-edge-inspection-inspection-worker" \
          ./packaging/inspection-worker-deb/build-deb.sh
          
      # ============================================================
      # LinuxEdgeInspection.Management
      # ============================================================

      # linux-x64向けpublishに必要な依存関係を復元します。
      - name: Restore Management dependencies
        run: >
          dotnet restore
          src/Management/LinuxEdgeInspection.Management/LinuxEdgeInspection.Management.csproj
          --runtime linux-x64

      # LinuxEdgeInspection.Managementを
      # linux-x64向けにpublishします。
      #
      # framework-dependent構成のため、実機には
      # ASP.NET Core Runtime 10が必要です。
      - name: Publish Management
        run: >
          dotnet publish
          src/Management/LinuxEdgeInspection.Management/LinuxEdgeInspection.Management.csproj
          --configuration Release
          --runtime linux-x64
          --self-contained false
          --no-restore
          --output artifacts/publish/linux-edge-inspection-management
          -p:Version=${{ env.VERSION }}
          -p:SourceRevisionId=${{ github.sha }}
          -p:ContinuousIntegrationBuild=true

      # 配布不要な開発用ファイルを削除します。
      - name: Clean Management publish output
        shell: bash
        run: |
          rm -f \
            artifacts/publish/linux-edge-inspection-management/appsettings.Development.json

          find artifacts/publish/linux-edge-inspection-management \
            -maxdepth 1 \
            -type f \
            -name "*.pdb" \
            -delete

      # ManagementのSBOM出力先を作成します。
      - name: Create Management SBOM directory
        run: |
          mkdir -p artifacts/sbom/management

      # Managementのpublish成果物を基準にSBOMを生成します。
      - name: Generate Management SBOM
        shell: bash
        run: |
          sbom-tool generate \
            -b "${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-management" \
            -m "${GITHUB_WORKSPACE}/artifacts/sbom/management" \
            -pn "LinuxEdgeInspection.Management" \
            -pv "${VERSION}" \
            -ps "mono-tec"

      # ManagementのSBOMからOSSライセンスNoticeを生成します。
      - name: Generate Management OSS notice
        shell: bash
        run: |
          source "${RUNNER_TEMP}/onot-venv/bin/activate"

          rm -rf artifacts/license/linux-edge-inspection-management
          mkdir -p artifacts/license/linux-edge-inspection-management

          onot generate \
            -i artifacts/sbom/management/_manifest/spdx_2.2/manifest.spdx.json \
            -f markdown \
            --output-dir artifacts/license/linux-edge-inspection-management

      # Managementのライセンス関連ファイルを
      # Debianパッケージへ格納しやすい形へ整理します。
      - name: Prepare Management compliance files
        shell: bash
        run: |
          COMPLIANCE_DIR="artifacts/compliance/linux-edge-inspection-management"

          rm -rf "${COMPLIANCE_DIR}"
          mkdir -p "${COMPLIANCE_DIR}"

          cp LICENSE "${COMPLIANCE_DIR}/LICENSE"

          NOTICE_FILE="$(find \
            artifacts/license/linux-edge-inspection-management \
            -maxdepth 1 \
            -type f \
            -name "*.md" \
            | head -n 1)"

          if [[ -z "${NOTICE_FILE}" ]]; then
            echo "Management OSS Notice file was not generated."
            exit 1
          fi

          cp \
            "${NOTICE_FILE}" \
            "${COMPLIANCE_DIR}/THIRD-PARTY-NOTICES.md"

          cp \
            artifacts/sbom/management/_manifest/spdx_2.2/manifest.spdx.json \
            "${COMPLIANCE_DIR}/sbom.spdx.json"

          echo "Management compliance files:"
          find "${COMPLIANCE_DIR}" -maxdepth 1 -type f -print

      # publish成果物からManagement用debパッケージを作成します。
      - name: Create Management Debian package
        shell: bash
        run: |
          chmod +x packaging/management-deb/build-deb.sh

          PACKAGE_VERSION="${VERSION}" \
          PACKAGE_ARCHITECTURE="amd64" \
          RUNTIME_IDENTIFIER="linux-x64" \
          PUBLISH_DIR="${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-management" \
          COMPLIANCE_DIR="${GITHUB_WORKSPACE}/artifacts/compliance/linux-edge-inspection-management" \
          ./packaging/management-deb/build-deb.sh

      # ============================================================
      # LinuxEdgeInspection.Management.Api
      # ============================================================

      # linux-x64向けpublishに必要な依存関係を復元します。
      - name: Restore Management API dependencies
        run: >
          dotnet restore
          src/Management/LinuxEdgeInspection.Management.Api/LinuxEdgeInspection.Management.Api.csproj
          --runtime linux-x64

      # LinuxEdgeInspection.Management.Apiを
      # linux-x64向けにpublishします。
      #
      # framework-dependent構成のため、実機には
      # ASP.NET Core Runtime 10が必要です。
      - name: Publish Management API
        run: >
          dotnet publish
          src/Management/LinuxEdgeInspection.Management.Api/LinuxEdgeInspection.Management.Api.csproj
          --configuration Release
          --runtime linux-x64
          --self-contained false
          --no-restore
          --output artifacts/publish/linux-edge-inspection-management-api
          -p:Version=${{ env.VERSION }}
          -p:SourceRevisionId=${{ github.sha }}
          -p:ContinuousIntegrationBuild=true

      # 配布不要な開発用ファイルを削除します。
      - name: Clean Management API publish output
        shell: bash
        run: |
          rm -f \
            artifacts/publish/linux-edge-inspection-management-api/appsettings.Development.json

          find artifacts/publish/linux-edge-inspection-management-api \
            -maxdepth 1 \
            -type f \
            -name "*.pdb" \
            -delete

      # Management APIのSBOM出力先を作成します。
      - name: Create Management API SBOM directory
        run: |
          mkdir -p artifacts/sbom/management-api

      # Management APIのpublish成果物を基準にSBOMを生成します。
      - name: Generate Management API SBOM
        shell: bash
        run: |
          sbom-tool generate \
            -b "${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-management-api" \
            -m "${GITHUB_WORKSPACE}/artifacts/sbom/management-api" \
            -pn "LinuxEdgeInspection.Management.Api" \
            -pv "${VERSION}" \
            -ps "mono-tec"

      # Management APIのSBOMからOSSライセンスNoticeを生成します。
      - name: Generate Management API OSS notice
        shell: bash
        run: |
          source "${RUNNER_TEMP}/onot-venv/bin/activate"

          rm -rf artifacts/license/linux-edge-inspection-management-api
          mkdir -p artifacts/license/linux-edge-inspection-management-api

          onot generate \
            -i artifacts/sbom/management-api/_manifest/spdx_2.2/manifest.spdx.json \
            -f markdown \
            --output-dir artifacts/license/linux-edge-inspection-management-api

      # Management APIのライセンス関連ファイルを
      # Debianパッケージへ格納しやすい形へ整理します。
      - name: Prepare Management API compliance files
        shell: bash
        run: |
          COMPLIANCE_DIR="artifacts/compliance/linux-edge-inspection-management-api"

          rm -rf "${COMPLIANCE_DIR}"
          mkdir -p "${COMPLIANCE_DIR}"

          cp LICENSE "${COMPLIANCE_DIR}/LICENSE"

          NOTICE_FILE="$(find \
            artifacts/license/linux-edge-inspection-management-api \
            -maxdepth 1 \
            -type f \
            -name "*.md" \
            | head -n 1)"

          if [[ -z "${NOTICE_FILE}" ]]; then
            echo "Management API OSS Notice file was not generated."
            exit 1
          fi

          cp \
            "${NOTICE_FILE}" \
            "${COMPLIANCE_DIR}/THIRD-PARTY-NOTICES.md"

          cp \
            artifacts/sbom/management-api/_manifest/spdx_2.2/manifest.spdx.json \
            "${COMPLIANCE_DIR}/sbom.spdx.json"

          echo "Management API compliance files:"
          find "${COMPLIANCE_DIR}" -maxdepth 1 -type f -print

      # publish成果物からManagement API用debパッケージを作成します。
      - name: Create Management API Debian package
        shell: bash
        run: |
          chmod +x packaging/management-api-deb/build-deb.sh

          PACKAGE_VERSION="${VERSION}" \
          PACKAGE_ARCHITECTURE="amd64" \
          RUNTIME_IDENTIFIER="linux-x64" \
          PUBLISH_DIR="${GITHUB_WORKSPACE}/artifacts/publish/linux-edge-inspection-management-api" \
          COMPLIANCE_DIR="${GITHUB_WORKSPACE}/artifacts/compliance/linux-edge-inspection-management-api" \
          ./packaging/management-api-deb/build-deb.sh


      # ============================================================
      # 生成結果の確認
      # ============================================================

      # 作成されたdebパッケージを確認します。
      - name: Inspect generated Debian packages
        shell: bash
        run: |
          ls -la artifacts/packages

          dpkg-deb --info \
            "artifacts/packages/linux-edge-inspection-runtime_${VERSION}_amd64.deb"

          dpkg-deb --info \
            "artifacts/packages/linux-edge-inspection-capture-request-listener_${VERSION}_amd64.deb"

          dpkg-deb --info \
            "artifacts/packages/linux-edge-inspection-inspection-worker_${VERSION}_amd64.deb"
          
          dpkg-deb --info \
            "artifacts/packages/linux-edge-inspection-management_${VERSION}_amd64.deb"

          dpkg-deb --info \
            "artifacts/packages/linux-edge-inspection-management-api_${VERSION}_amd64.deb"


      # deb内部に意図したファイルが配置されていることを確認します。
      - name: Inspect Debian package contents
        shell: bash
        run: |
          dpkg-deb --contents \
            "artifacts/packages/linux-edge-inspection-runtime_${VERSION}_amd64.deb"

          dpkg-deb --contents \
            "artifacts/packages/linux-edge-inspection-capture-request-listener_${VERSION}_amd64.deb"

          dpkg-deb --contents \
            "artifacts/packages/linux-edge-inspection-inspection-worker_${VERSION}_amd64.deb"

          dpkg-deb --contents \
            "artifacts/packages/linux-edge-inspection-management_${VERSION}_amd64.deb"

          dpkg-deb --contents \
            "artifacts/packages/linux-edge-inspection-management-api_${VERSION}_amd64.deb"


      # SBOM生成結果を確認します。
      - name: Inspect generated SBOMs
        shell: bash
        run: |
          find artifacts/sbom -type f -print

      # Compliance生成結果を確認します。
      - name: Inspect generated compliance files
        shell: bash
        run: |
          find artifacts/compliance -type f -print

      # ============================================================
      # 成果物の登録
      # ============================================================

      # Actionsの実行結果からダウンロードできるように、
      # 5つのdebパッケージをWorkflow Artifactへ登録します。
      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v7
        with:
          name: LinuxEdgeInspection-linux-x64
          path: |
            artifacts/packages/linux-edge-inspection-runtime_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-capture-request-listener_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-inspection-worker_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-management_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-management-api_${{ env.VERSION }}_amd64.deb
          if-no-files-found: error
          retention-days: 14

      # タグに対応するGitHub Releaseを作成し、
      # 5つのdebパッケージを添付します。
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            artifacts/packages/linux-edge-inspection-runtime_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-capture-request-listener_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-inspection-worker_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-management_${{ env.VERSION }}_amd64.deb
            artifacts/packages/linux-edge-inspection-management-api_${{ env.VERSION }}_amd64.deb
          generate_release_notes: true